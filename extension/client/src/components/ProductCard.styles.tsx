import styled from "@emotion/styled"

export const Wrapper = styled.div`
	.ant-card-actions {
		flex-direction: column;
		gap: 4px;

		button,
		li {
			width: 100% !important;
			margin: 0;
		}
	}
`
